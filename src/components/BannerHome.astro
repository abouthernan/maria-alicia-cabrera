<section class="section">
  <div class="container">
    <div class="book">
      <img src="book-banner.png" alt="puede ser una caratula de una portada de un libro" class="book__img">
    </div>
    <article class="description">
      <h2 class="title">PENÉLOPE RETORNA AL AMOR</h2>
      <h3 class="subtitle">Nota de la autora</h3>
      <p class="note">Lorem ipsum dolor sit amet consectetur adipisicing elit. Libero aliquam quasi et rerum expedita autem laudantium voluptatum placeat esse quis quisquam aliquid repellat, odio sequi doloribus. Ducimus totam expedita sunt maxime! Libero et neque sapiente nobis illum inventore, magnam aliquam.</p>
      <nav class="nav">
        <a href="#">Seguir leyendo</a>
        <a href="#">Descargar</a>
        <a href="#">Ver más libros</a>
      </nav>
    </article>
  </div>
</section>

<style>
  .section {
    background: var(--cream);
    padding: 8rem 0 4rem;
  }

  .container {
    display: grid;
  }

  .book {
    width: 100%;
    height: 100%;
    max-width: 300px;
    max-height: 300px;
    margin: 0 auto;
  }

  .book__img {
    width: 100%;
    height: 100%;
    aspect-ratio: 1/1;
    object-fit: contain;
  }

  .title {
    font-size: var(--font-sm);
    text-align: center;
  }

  .subtitle {
    text-align: left;
  }

  .note {
    font-family: var(--font-body);
  }

  .nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin: 1.5rem auto;
  }

  .nav a {
    font-size: var(--font-normal);
    font-family: var(--font-body);
    color: var(--green);
    padding-bottom: 0.1rem;
    border-bottom: 1px solid var(--green);
  }

  @media (min-width: 768px) {
    .container {
      min-height: 90vh;
      place-items: center;
      grid-template-columns: 1fr 1fr;
    }

    .book {
      max-width: 500px;
      max-height: 500px;
    }

    .title {
      font-size: var(--font-md);
      text-align: left;
      font-weight: 600;
    }

    .nav {
      margin: 0;
      justify-content: flex-end;
      column-gap: 2rem;
    }
  }

</style>
